---
import '../styles/global.css';
import SeoHead from '../components/SeoHead.astro';

interface PostLink {
  slug: string;
  title: string;
}

interface Props {
  title: string;
  gist: string;
  category: string;
  topic?: string;
  readingTime: number;
  pubDate: Date;
  slug?: string;
  newer?: PostLink | null;
  older?: PostLink | null;
}

const { title, gist, category, topic, readingTime, pubDate, slug, newer, older } = Astro.props;

const dateStr = pubDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' });
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title} — Lit Bulb Lab</title>
    <meta name="description" content={gist} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <SeoHead
      title={title}
      description={gist}
      image={slug ? `/og/${slug}.png` : undefined}
      type="article"
    />
  </head>
  <body>
    <header class="wrap site-head">
      <a href="/" class="logo">Lit Bulb Lab</a>
      <nav>
        <a href="/#topics">Topics</a>
        <a href="/#why">Why</a>
        <a href="/posts">Posts</a>
      </nav>
    </header>

    <main>
      <article class="wrap post">
        <div class="post-meta">
          <span class="category-badge">{category}</span>
          {topic && <span class="topic-label">{topic}</span>}
          <span class="meta-divider" aria-hidden="true">·</span>
          <span class="muted">{readingTime} min read</span>
          <span class="meta-divider" aria-hidden="true">·</span>
          <time datetime={pubDate.toISOString()} class="muted">{dateStr}</time>
        </div>

        <h1>{title}</h1>

        <p class="gist">{gist}</p>

        <div class="post-body">
          <slot />
        </div>

        <div class="share">
          <span class="share-label">Share</span>
          <a
            href={`https://x.com/intent/tweet?text=${encodeURIComponent(title)}&url=${encodeURIComponent(new URL(Astro.url.pathname, Astro.site).href)}`}
            target="_blank"
            rel="noopener noreferrer"
            class="share-link"
          >Post on X</a>
          <span class="follow-note">
            New post most days — follow
            <a href="https://www.instagram.com/litbulblab/" target="_blank" rel="noopener noreferrer">@litbulblab</a>
            or grab the <a href="/rss.xml">RSS feed</a>.
          </span>
        </div>

        {(older || newer) && (
          <nav class="post-nav" aria-label="More posts">
            {older ? (
              <a href={`/posts/${older.slug}`} class="post-nav-link">
                <span class="post-nav-dir">← Older</span>
                <span class="post-nav-title">{older.title}</span>
              </a>
            ) : <span />}
            {newer && (
              <a href={`/posts/${newer.slug}`} class="post-nav-link post-nav-next">
                <span class="post-nav-dir">Newer →</span>
                <span class="post-nav-title">{newer.title}</span>
              </a>
            )}
          </nav>
        )}
      </article>
    </main>

    <footer class="wrap site-foot">
      <span>Lit Bulb Lab</span>
      <span class="social-links">
        <a href="https://www.instagram.com/litbulblab/" target="_blank" rel="noopener noreferrer">@litbulblab</a>
        <span class="muted" aria-hidden="true">·</span>
        <a href="https://x.com/litbulblab" target="_blank" rel="noopener noreferrer">@litbulblab</a>
      </span>
    </footer>
  </body>
</html>

<style>
  .site-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-block: 1.5rem;
  }
  .logo {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: 1.3rem;
    text-decoration: none;
    color: var(--ink);
  }
  nav a { margin-left: 1.5rem; color: var(--ink-soft); text-decoration: none; font-weight: 700; }
  nav a:hover { color: var(--accent); }

  .share {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid var(--line);
    max-width: var(--measure);
  }
  .share-label { font-weight: 700; color: var(--ink-soft); font-size: 0.9rem; text-transform: uppercase; letter-spacing: 0.08em; }
  .share-link {
    background: var(--accent);
    color: #fff;
    padding: 0.45rem 1.1rem;
    border-radius: var(--radius);
    font-weight: 700;
    font-size: 0.9rem;
    text-decoration: none;
    transition: transform 0.15s ease;
  }
  .share-link:hover { transform: translateY(-1px); }
  .follow-note { color: var(--ink-soft); font-size: 0.9rem; }
  .follow-note a { color: var(--accent); font-weight: 700; text-decoration: none; }
  .follow-note a:hover { text-decoration: underline; }

  .post-nav {
    display: flex;
    justify-content: space-between;
    gap: 1.5rem;
    margin-top: 2rem;
    padding-top: 2rem;
    border-top: 1px solid var(--line);
    max-width: var(--measure);
  }
  .post-nav-link {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    text-decoration: none;
    color: inherit;
    max-width: 45%;
  }
  .post-nav-next { text-align: right; margin-left: auto; }
  .post-nav-dir {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--ink-soft);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .post-nav-title { font-weight: 700; transition: color 0.15s; }
  .post-nav-link:hover .post-nav-title { color: var(--accent); }

  .post { padding-block: clamp(2rem, 6vw, 4rem); }

  .post-meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
    font-size: 0.9rem;
    font-weight: 700;
  }
  .category-badge {
    background: var(--accent-soft);
    color: var(--accent);
    padding: 0.2rem 0.7rem;
    border-radius: 99px;
    font-size: 0.8rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .meta-divider { color: var(--line); }
  .muted { color: var(--ink-soft); font-weight: 400; }
  .topic-label { color: var(--ink-soft); font-weight: 700; }

  h1 {
    font-size: clamp(2.2rem, 7vw, 4.5rem);
    margin: 0 0 1.5rem;
    max-width: 22ch;
  }

  .gist {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--ink);
    background: var(--accent-soft);
    border-radius: var(--radius);
    padding: 1.1rem 1.4rem;
    margin: 0 0 3rem;
    max-width: var(--measure);
  }

  .post-body {
    max-width: var(--measure);
    font-family: var(--font-body);
  }

  /* Section headings inside post */
  .post-body :global(h2) {
    font-size: clamp(1.5rem, 3vw, 2rem);
    margin: 3rem 0 1rem;
    padding-top: 1rem;
    border-top: 1px solid var(--line);
  }
  .post-body :global(h3) {
    font-size: 1.2rem;
    margin: 2rem 0 0.75rem;
  }
  .post-body :global(p) { margin: 0 0 1.25rem; }
  .post-body :global(ul), .post-body :global(ol) {
    padding-left: 1.5rem;
    margin: 0 0 1.25rem;
  }
  .post-body :global(li) { margin-bottom: 0.5rem; }
  .post-body :global(strong) { color: var(--ink); }
  .post-body :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 2rem 0;
    font-size: 0.95rem;
  }
  .post-body :global(th) {
    text-align: left;
    padding: 0.6rem 1rem;
    background: var(--accent-soft);
    color: var(--accent);
    font-weight: 700;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  .post-body :global(td) {
    padding: 0.75rem 1rem;
    border-bottom: 1px solid var(--line);
    vertical-align: top;
  }
  .post-body :global(tr:last-child td) { border-bottom: none; }
  .post-body :global(tr:nth-child(even) td) { background: var(--paper-raised); }

  .post-body :global(img) {
    width: 100%;
    border-radius: var(--radius);
    margin: 1.5rem 0;
    display: block;
  }
  .post-body :global(code) {
    background: var(--paper-raised);
    border: 1px solid var(--line);
    border-radius: 4px;
    padding: 0.1em 0.4em;
    font-size: 0.9em;
  }

  .site-foot {
    display: flex;
    justify-content: space-between;
    padding-block: 2.5rem;
    margin-top: clamp(2rem, 6vw, 5rem);
    border-top: 1px solid var(--line);
    font-weight: 700;
  }
</style>
